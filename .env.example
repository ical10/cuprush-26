# Postgres connection string. Local dev expects a database created with
# `createdb worldcup_hilo` on the default Homebrew Postgres socket.
DATABASE_URL=postgresql://localhost:5432/worldcup_hilo

# Port the Hono server (API + production client) listens on.
PORT=3000

# full (default) = local all-in-one HTTP plus background loops; web = HTTP
# only. Railway keeps the app on web and runs `pnpm match-runner` by cron.
APP_RUNTIME_MODE=full

# privy (default) = real Privy verification; dev = local auth stub, must be
# set explicitly and refuses to run when NODE_ENV is prod-ish. Leaving this
# unset selects privy, which fails closed without PRIVY_APP_ID/SECRET below.
AUTH_MODE=dev

# TxLINE ingestion (devnet, service level 1). replay (default) streams the captured
# JSON fixtures in src/txline/fixtures/samples; live connects to the real
# TxLINE API with the credentials below. TXLINE_BASE_URL is the origin only
# (e.g. https://txline-dev.txodds.com), WITHOUT the /api suffix — the client
# appends paths itself. TXLINE_API_KEY is the durable X-Api-Token; the guest
# JWT is minted automatically at runtime.
TXLINE_MODE=replay
TXLINE_API_KEY=
TXLINE_BASE_URL=
# ms between replayed events; 0 applies every captured event immediately.
TXLINE_REPLAY_INTERVAL_MS=1500
# Optional alternate directory of captured replay JSON files (README smoke test).
#TXLINE_FIXTURES_DIR=
# Ingest competition filter. A single TxLINE CompetitionId; a fixture is
# ingested only if its CompetitionId matches. 72 = World Cup on the devnet feed
# (the wanted content — old WC matches the feed replays), 430 = Friendlies (junk).
# Empty/unset = allow all (default; preserves dev/replay behavior).
TXLINE_COMPETITION_ID=72
# One-off junk cleanup only (NOT used by ingest): `pnpm cleanup:fixtures` reads
# TXLINE_TEAM_ALLOWLIST — comma-separated team names, case-insensitive exact
# match after trim — to cancel pre-existing non-World-Cup rows that predate the
# competition filter and carry a NULL competition_id (which competition-based
# cleanup can't identify). Refuses to run when empty. See src/db/cleanup-fixtures.ts.
#TXLINE_TEAM_ALLOWLIST=Spain,Argentina,England,France

# Runner chain writes fail closed. Keep disabled until the real Solana
# adapter and credentials have been reviewed; enabled requires CHAIN_MODE=solana.
MATCH_RUNNER_CHAIN_WRITES=disabled

# Privy app credentials (email OTP + embedded Solana wallet + sponsorship).
# PRIVY_APP_ID / _SECRET are server-side (token verification). VITE_PRIVY_APP_ID
# is the same app id re-exposed to the browser bundle (public, safe) and is
# required when VITE_AUTH_MODE=privy. VITE_AUTH_MODE picks the client auth path
# (dev stub vs real Privy) and should track AUTH_MODE.
PRIVY_APP_ID=
PRIVY_APP_SECRET=
VITE_AUTH_MODE=dev
VITE_PRIVY_APP_ID=

# Private key of the app's authorization keypair (registered in the Privy
# dashboard). Required ONLY by `pnpm provision:agents`, which creates the AI
# cohort's Privy server wallets — an authorized wallet-API action. The command
# fails closed with a clear message if this (or PRIVY_APP_ID/_SECRET) is unset.
# Never needed by the web app or token verification. Keep it out of the client
# bundle (no VITE_ prefix).
PRIVY_AUTHORIZATION_KEY=

# OpenRouter key for LLM-assisted question selection (small model, background
# only, never in a request path). Off by default — set LLM_SELECTOR=on and
# provide OPENROUTER_API_KEY to enable; otherwise the deterministic template
# fallback is always used.
OPENROUTER_API_KEY=
LLM_SELECTOR=off
OPENROUTER_MODEL=openai/gpt-4o-mini

# On-chain adapter. CHAIN_MODE=solana activates the real devnet adapter (the
# production chain path — devnet-only product decision); anything else uses
# the in-memory stub. CUPRUSH_PROGRAM_ID is the world_cup_hilo Anchor program
# deployed to devnet: 9u7uuj7S8kMon564b4TA8Gc7RaYXSC5QgjDz8fFgmGCU
# SOLANA_PRIVATE_KEY is the question authority + fee payer (a funded devnet
# wallet), as a JSON byte array or base58-encoded 64-byte secret key. It is
# also the gate for `pnpm test:integration`'s devnet suite, which auto-skips
# when CUPRUSH_PROGRAM_ID or SOLANA_PRIVATE_KEY is unset.
CHAIN_MODE=stub
SOLANA_RPC_URL=https://api.devnet.solana.com
CUPRUSH_PROGRAM_ID=9u7uuj7S8kMon564b4TA8Gc7RaYXSC5QgjDz8fFgmGCU
SOLANA_PRIVATE_KEY=
# Build-time only (anchor build/deploy, not a server runtime var): the PUBLIC
# key of the server wallet, baked into the program as the create_question
# authority allowlist. Never put a private key here.
#CUPRUSH_AUTHORITY_PUBKEY=
